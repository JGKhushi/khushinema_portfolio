# Base URL for the portfolio API. In dev, leave unset — Vite proxies /api to
# http://localhost:5000 (see vite.config.ts). In production, point this at the
# deployed backend, e.g. https://khushi-portfolio-api.onrender.com/api/v1
# VITE_API_BASE=/api/v1
